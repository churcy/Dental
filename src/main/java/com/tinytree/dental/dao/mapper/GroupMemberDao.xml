<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tinytree.dental.dao.GroupMemberDao">

    <select id="getFriendByUserId" resultType="java.util.HashMap" parameterType="java.util.HashMap">

        SELECT gi.name AS groupName,gm.username AS groupMemberUsername,status AS groupMemberStatus
        FROM group_info gi
        LEFT JOIN group_members gm
        ON gi.id = gm.group_id
        WHERE gi.user_id = #{userId}

    </select>

</mapper>